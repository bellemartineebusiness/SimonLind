# Web3Forms API key — get yours free at https://web3forms.com
# Used server-side only (no NEXT_PUBLIC_ prefix = never exposed in client bundle).
# The repo ships with a compile-time fallback so the form works out of the box,
# but setting this variable overrides it at runtime.
WEB3FORMS_KEY=604b7168-08c6-4b2a-9f46-a3ef1581e224
